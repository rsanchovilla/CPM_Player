; gtload loads a HP74XX to GSX driver, plus the GSX driver
; it is completly transparent to GrafTalk that think it is
; sending HP/GL plotter commands to printer port
; gtload needs the mandatoy GSX files: GSX.SYS, 
; ASSIGN.SYS, and DD*.PRL driver
; gtload will also load and run GRAFTALK.COM
gtload

