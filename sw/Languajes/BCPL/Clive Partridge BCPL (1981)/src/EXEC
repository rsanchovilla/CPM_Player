get "libhdr"


LET start() BE
$(
   LET prog,args = VEC 64, VEC 64
   writes(" Program? ")
   readline(prog,128)
   writes("*n Command line? ")
   readline(args,128) 
   execute(prog,args)    
$)

