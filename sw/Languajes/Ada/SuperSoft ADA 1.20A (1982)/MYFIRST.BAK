procedure myfirst is
  
  function distance(a,b,c : integer) return integer is
   begin

    return a*a+b*b+c*c;

  end distance;


begin

   put("the distance is ",distance(3,4,5));
   newline;

end myfirst;
